import React from 'react'
import { shallow } from 'enzyme'
import Header from './header'

describe('<Header />', () => {
  it('Renders something', () => {
    const isWon = []
    const lastGuess = {}
    const correctAnswer = '';
    shallow(<Header />)
  })
})